<!-- <div class="container">
 --><div class="studio-address-map">
  <div class="studio-info">
    <h4><%= @course.studio.name %></h4>
    <p><%= @course.studio.address %></p>
  </div>
  <div class="studio-map-info">
    <p> <%= @json_distance%> away</p>
  </div>
</div>
<!-- </div> -->
<div id="map" style="width: 100%; height: 200px;">

  <% content_for(:after_js) do %>
    <%= javascript_tag do %>
        $(document).ready(function() {
        var handler = Gmaps.build('Google');
        handler.buildMap({ internal: { id: 'map' } }, function() {
        markers = handler.addMarkers(<%= raw @hash.to_json %>);
        handler.bounds.extendWith(markers);
        handler.fitMapToBounds();
        if (markers.length == 0) {
        <!-- handler.getMap().setZoom(2); -->
      } else if (markers.length == 1) {
      handler.getMap().setZoom(14);
    }
    });
    });
    <% end %>
  <% end %>
</div>
<%#= @request %>


<!-- <h3><%#= @course.studio.name %></h3> -->
<%#= image_tag "https://maps.googleapis.com/maps/api/staticmap?zoom=15&size=375x200&sensor=false&key=#{ENV['GOOGLE_API_SERVER_KEY']}&maptype=roadmap&markers=color:red|@course.studio.address"  %>
