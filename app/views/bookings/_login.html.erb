<% if current_user %>
  <button class="new-booking-button" data-toggle="modal" data-target="#bookingLoginModal">
    <h3 class="text-center">Join</h3>
  </button>
<% else %>
  <%= link_to t(".sign_in", default: "Login To Book"), new_user_session_path, class: "new-booking-button" %>
<% end %>

